import { createStore } from "vuex";

export default createStore({
  state: {
    prompt: "What does this Kanji mean?",
    content: "日",
  },
  getters: {},
  mutations: {},
  actions: {},
  modules: {},
});
